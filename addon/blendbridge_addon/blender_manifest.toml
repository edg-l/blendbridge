schema_version = "1.0.0"

id = "blendbridge_addon"
version = "0.1.0"
name = "BlendBridge"
tagline = "MCP bridge - exposes Blender to AI agents via HTTP"
maintainer = "BlendBridge"
type = "add-on"

tags = ["Development"]

blender_version_min = "5.0.0"

license = [
  "SPDX:GPL-3.0-or-later",
]

[permissions]
network = "HTTP server for MCP communication on localhost"
